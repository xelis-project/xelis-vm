func bool_test(): bool {
    return true
}

entry main() {
    if (!bool_test()) && true { // TODO fix order
        return 1
    }

    let i: int = bool_test() ? 10 : 0
    if i == 10 {
        return 0
    } else {
        return 2
    }
}