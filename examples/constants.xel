func hello(): string {
    return "Hello World!"
}

const ZERO: int = 0
const HELLO_WORLD: string = hello()

entry main() {
    let message: string = HELLO_WORLD + " " + ZERO
    println(message)

    return 0
}