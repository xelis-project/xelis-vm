func add(a: byte, b: byte): byte {
    return a + b
}

entry main() {
    let a: byte = 5
    let b: byte = true ? 250 : 10
    println(add(a, b))

    let res: byte = b << 1
    println(res)

    let divide: int = 10 / 0
    return 0
}