fn add(a: u8, b: u8) -> u8 {
    return a + b
}

entry main() {
    let a: u8 = 5
    let b: u8 = true ? 250 : 10
    println(add(a, b))

    let res: u8 = b << 1
    println(res)

    let divide: u64 = 10 / 0
    return 0
}